"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{entrar}from"@/servicos/api";import{useAutenticacao}from"@/contextos/AutenticacaoContexto";export default function Login(){const[email,setEmail]=useState(''),[senha,setSenha]=useState(''),[erro,setErro]=useState(''),[enviando,setEnviando]=useState(false);const{iniciar}=useAutenticacao();const router=useRouter();const enviar=async(e:React.FormEvent)=>{e.preventDefault();setEnviando(true);setErro('');try{const sessao=await entrar(email,senha);iniciar(sessao);router.replace(sessao.tipoConta==='PESSOAL'?'/pessoal/visao-geral':'/empresarial/visao-geral')}catch(x){setErro(x instanceof Error?x.message:'Não foi possível entrar.')}finally{setEnviando(false)}};return <main className="login"><form className="cartao-login" onSubmit={enviar}><div className="marca">OnKash <span>Finance</span></div><h1>Bem-vindo</h1><p>Cuide das suas finanças de forma simples.</p><label className="campo">E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email"/></label><label className="campo">Senha<input type="password" value={senha} onChange={e=>setSenha(e.target.value)} required autoComplete="current-password"/></label>{erro&&<p className="mensagem erro" role="alert">{erro}</p>}<button className="botao" disabled={enviando}>{enviando?'Entrando...':'Entrar'}</button></form></main>}
