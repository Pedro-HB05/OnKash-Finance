# URL da API usada pelo servidor Next.js (não é exposta ao navegador).
API_URL=https://onkash-finance.onrender.com

# Deixe vazio para usar o proxy `/api` do Next e evitar bloqueio de CORS.
NEXT_PUBLIC_API_URL=
